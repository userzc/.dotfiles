# Bind eclim to all interfaces
nailgun.server.host=0.0.0.0

# Specifies the port that nailgun / eclim listens on for client requests.
nailgun.server.port=9091

# Specidies the workspace directory to use
# See #ECLIPSE_HOME/configuration/config.ini for other osgi properties.
osgi.instance.area.default=@user.home/workspace

# increase heap size
-Xmx256M

# increase perm gen size
-XX:PermSize=64m
-XX:MaxPermSize=128m